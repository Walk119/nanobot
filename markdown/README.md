# markdown

This is a code bundle for markdown. The original project is available at https://www.figma.com/design/7FgyAg070sAn75tZrK4LTd/markdown.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

# todo

* 对话框输出接口，选中部分插入到markdown
* 设计多套提示词，展示提示词列表和内容，切换提示词
* 同一个问题，多个提示词，并行执行agent loop
